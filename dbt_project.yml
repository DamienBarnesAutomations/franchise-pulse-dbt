name: 'franchise_pulse_dbt'
version: '1.0.0'
config-version: 2

profile: 'franchise_pulse'

model-paths: ["models"]
test-paths: ["tests"]
macro-paths: ["macros"]
analysis-paths: ["analyses"]

models:
  franchise_pulse_dbt:
    staging:
      +materialized: view
      +schema: staging
    intermediate:
      +materialized: view
      +schema: intermediate
    marts:
      +materialized: view
      +schema: marts
      fct_sales:
        +materialized: incremental
